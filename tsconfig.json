{
  "compilerOptions": {
    "target": "ES2023",
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "jsx": "react-jsx",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "types": ["node"]
  },
  "// tests/**/*.tsx": "AJOUTÉ LE 2026-08-13. Le motif se termine par `.ts`, donc TypeScript le prend au pied de la lettre : `tests/**/*.ts` ne ramassait PAS les `.tsx`. `tests/scelles/65a-ecran.test.tsx` échappait à `npm run typecheck` — un test scellé hors de portée d'une des quatre commandes. Vérifié : l'ajout ne révèle aucune erreur, il ferme un angle mort.",
  "include": [
    "catalog/**/*.ts",
    "tests/**/*.ts",
    "tests/**/*.tsx",
    "app/src/**/*.ts",
    "app/src/**/*.tsx"
  ],
  "// tests/scelles/sondes-66": "EXCLU EXPRÈS, et il faut que ça reste vrai. Ce dossier contient des fichiers dont on mesure la COMPILATION : l'un d'eux est écrit pour ÉCHOUER. S'il rentrait ici, `npm run typecheck` serait rouge en permanence et la seule façon de le rendre vert serait de défaire le lot 66. `tests/scelles/66.test.ts` les compile lui-même, avec leurs propres tsconfig.",
  "exclude": ["node_modules", "app/public", "tests/scelles/sondes-66"]
}
